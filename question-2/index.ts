// Question 83: Converting to Upper and Lower Case: Create a function that takes a string, converts it to uppercase, then to lowercase, and logs both.

// Explain & TIP: The .toUpperCase() and .toLowerCase() methods allow you to change the case of all letters in a string, useful for formatting or comparing strings

function string_Methods(str) {
  console.log("string is:", str);

  console.log("string in uppercase:", str.toUpperCase());

  console.log("string in lowercase:", str.toLowerCase());
}
string_Methods("I LoVe mY COuntRY");
//Author-"HUMA MOHSIN"
